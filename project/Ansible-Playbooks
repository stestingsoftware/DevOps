Ansible Controller ssh -i "createAnsibleAssignment.pem" ubuntu@ec2-18-205-154-78.compute-1.amazonaws.com
Ansible Node ssh -i "createAnsibleAssignment.pem" ubuntu@ec2-54-146-5-123.compute-1.amazonaws.com


---------STEPS-Installing Ansible in Ubuntu-------------
check the python-- In ubuntu python is preinstalled
$ sudo apt update
$ sudo apt install software-properties-common
$ sudo add-apt-repository --yes --update ppa:ansible/ansible
$ sudo apt install ansible


-------- CONNECTION------
mkdir ansible-demo
cd ansible-demo/

